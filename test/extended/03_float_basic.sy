int main() {
    float a = 5.5;
    float b = 2.0;
    
    putfloat(a + b); putch(32); // 7.5
    putfloat(a - b); putch(32); // 3.5
    putfloat(a * b); putch(32); // 11.0
    putfloat(a / b); putch(10); // 2.75

    return 0;
}